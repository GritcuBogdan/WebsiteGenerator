---
id: forgot-password-002
requires: []
---
Forgetting a password for an account at {{BRAND_NAME}} is a common and easily fixed situation, provided the recovery steps are followed in the right order. Start from the official sign in page and select the password recovery option rather than attempting to guess variations of the old password, since repeated failed attempts can trigger a temporary account lock. Entering the registered email address triggers a recovery message, which should arrive within a few minutes.

When the message arrives, check that the sender address and branding match the official platform before clicking anything inside it, since recovery emails are a common target for imitation. Following the link leads to a page for setting a new password, which should be unique rather than reused from another account, and ideally combines letters, numbers, and a reasonable length to stay secure.

After setting the new password, log in once to confirm it works, and take a moment to review recent account activity for anything unfamiliar. If no recovery email arrives after a reasonable wait, double-check the entered address for typos before requesting another attempt, since a single character error is the most common reason a recovery message never appears.
